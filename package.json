{
  "name": "worker-email",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish --env production",
    "start": "wrangler dev"
  },
  {
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240716.0",
    "itty-router": "^3.3.12",
    "zod": "^3.23.8"
    // 移除 better-sqlite3
  }
}
